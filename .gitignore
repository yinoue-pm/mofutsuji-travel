# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# 生成物（PDF・スクショは重いので追跡しない / HTML は公開のためコミットする）
dist/*.pdf
dist/*.png
# ビルド時に root の assets/ からコピーされる複製（追跡不要）
dist/assets/

# OS
.DS_Store
Thumbs.db

# 画像取り込み用の一時退避（追跡不要）
.stage/
